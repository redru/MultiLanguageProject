/**
 * Created by admin on 23/10/2016.
 */
"use strict";

function Script(name, path, type, src) {
    this.name = name ? name : '';
    this.path = path ? path : '';
    this.type = type ? type : '';
    this.src = src ? src : '';
}
